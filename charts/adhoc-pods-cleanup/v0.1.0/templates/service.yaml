apiVersion: v1
kind: Service
metadata:
  name: {{ include "adhoc-pods-cleanup.fullname" . }}
  labels:
    {{- include "adhoc-pods-cleanup.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "adhoc-pods-cleanup.selectorLabels" . | nindent 4 }}
