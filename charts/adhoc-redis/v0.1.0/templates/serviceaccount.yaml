{{- if .Values.serviceAccount.create -}}
apiVersion: v2
kind: ServiceAccount
metadata:
  name: {{ include "adhoc-redis.serviceAccountName" . }}
  labels:
    {{- include "adhoc-redis.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
