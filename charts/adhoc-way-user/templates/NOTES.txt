State volume for user {{ .Values.user.email | default .Values.user.id | default "(unset)" }}.

Claim: {{ include "adhoc-way-user.claimName" . }} ({{ .Values.state.size }})

Each surface of this user mounts it as:

  helm install <prefix> adhoc-dev/adhoc-way-instance \
    --set state.existingClaim={{ include "adhoc-way-user.claimName" . }} \
    ...

{{ if .Values.state.retainOnUninstall }}Uninstalling this release KEEPS the claim
(there is no backup of it). To actually delete the data:

  kubectl delete pvc {{ include "adhoc-way-user.claimName" . }} -n {{ .Release.Namespace }}
{{ else }}⚠️  retainOnUninstall is off: `helm uninstall` DELETES the user's files.
{{ end }}
